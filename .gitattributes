# Normalize line endings: LF in the repository, native on checkout where needed.
* text=auto eol=lf

# Windows batch scripts require CRLF to run correctly.
*.bat text eol=crlf

# Binary assets must never be normalized.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.ttf binary
*.otf binary
*.zip binary
