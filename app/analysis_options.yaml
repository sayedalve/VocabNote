# App package analysis: inherits the workspace-wide strict configuration
# (very_good_analysis + strict language modes) so the app shell is held to
# the same standard as the core and feature packages.
include: ../analysis_options.yaml
