name: core_srs
description: Pure-Dart SM-2 spaced repetition scheduling (VocabNote domain core).
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
