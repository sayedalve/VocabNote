name: core_storage
description: VocabNote secure key-value storage (OS credential vault).
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.3.1
  path: ^1.9.1
  path_provider: ^2.1.6
  riverpod: ^3.3.2
  riverpod_annotation: ^4.0.3

dev_dependencies:
  build_runner: ^2.13.0
  flutter_test:
    sdk: flutter
  riverpod_generator: ^4.0.4
