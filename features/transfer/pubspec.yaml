name: feature_transfer
description: VocabNote data transfer feature (legacy v5 import; JSON/CSV/Markdown/Anki/DOCX transfer).
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  archive: ^3.6.1
  core_db:
    path: ../../core/db
  core_design_system:
    path: ../../core/design_system
  drift: ^2.34.2
  file_selector: ^1.1.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.2
  freezed_annotation: ^3.1.0
  path: ^1.9.1
  riverpod_annotation: ^4.0.3
  sqlite3: ^3.5.0
  xml: ^6.5.0

dev_dependencies:
  build_runner: ^2.13.0
  flutter_test:
    sdk: flutter
  freezed: ^3.2.5
  riverpod_generator: ^4.0.4
