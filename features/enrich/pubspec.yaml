name: feature_enrich
description: VocabNote AI enrichment feature (offline queue, capture input).
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  core_ai:
    path: ../../core/ai
  core_db:
    path: ../../core/db
  core_design_system:
    path: ../../core/design_system
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.2
  freezed_annotation: ^3.1.0
  riverpod_annotation: ^4.0.3

dev_dependencies:
  build_runner: ^2.13.0
  flutter_test:
    sdk: flutter
  freezed: ^3.2.5
  riverpod_generator: ^4.0.4
